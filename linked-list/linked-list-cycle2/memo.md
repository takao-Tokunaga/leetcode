# step1

vector で保存して見つかった時にアドレスを返せば良さそう
vector は find が使えないみたい
for 文で見つかったら返す
Accept された
時間計算量は O(n^2)
空間計算量は O(n)
大分遅いやり方かも

# step2

他の人の解答を見る
current でアドレスを返せるので vector を使う必要はなかった
141 とあまり変わらなかった
unordered_set にしたことで空間計算量 O(1)に

# step3

3 回解いて終了
